# Local standalone server (pnpm --filter @lmxcloud/pdf-extract dev)
PORT=8787
HOST=0.0.0.0

# Prefer the shared host for multi-tool / MCP local runs:
#   pnpm dev:tools-host
#   PDF_EXTRACT_URL=http://127.0.0.1:8080/pdf-extract
